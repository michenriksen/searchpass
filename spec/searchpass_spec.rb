require 'spec_helper'

describe Searchpass do
  it 'has a version number' do
    expect(Searchpass::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
